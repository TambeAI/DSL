import pandas as pd 
import numpy as np 
from sklearn.model_selection import train_test_split 
from sklearn.linear_model import LinearRegression 
from sklearn.metrics import mean_squared_error, r2_score 
# Load dataset 
df = pd.read_csv("BostonHousing.csv") 
print("----- DATA PREVIEW -----") 
print(df.head()) 
# Handle missing values (IMPORTANT FIX) 
df = df.fillna(df.mean()) 
# Split data 
X = df.drop("MEDV", axis=1) 
y = df["MEDV"] 
# Train-test split 
X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.2, random_state=0) 
# Model 
model = LinearRegression() 
model.fit(X_train, y_train) 
# Prediction 
y_pred = model.predict(X_test) 
# Results 
print("\n----- RESULTS -----") 
print("MSE:", mean_squared_error(y_test, y_pred)) 
print("R2 Score:", r2_score(y_test, y_pred)) 
import matplotlib.pyplot as plt 
plt.scatter(y_test, y_pred) 
plt.xlabel("Actual Price") 
plt.ylabel("Predicted Price") 
plt.title("Actual vs Predicted Prices") 
# line for perfect prediction 
plt.plot(y_test, y_test) 
plt.show() 





================ PROGRAM EXPLANATION ================

1. import pandas as pd
Imports pandas library.

Purpose:
- read dataset
- manipulate data
- perform analysis

2. import numpy as np
Imports NumPy library.

Purpose:
- numerical operations
- mathematical calculations

3. from sklearn.model_selection import train_test_split
Imports train_test_split function from scikit-learn.

Purpose:
Split dataset into:
- training data
- testing data

4. from sklearn.linear_model import LinearRegression
Imports Linear Regression model.

Purpose:
Used for predicting continuous numerical values.

5. from sklearn.metrics import mean_squared_error, r2_score
Imports evaluation metrics.

mean_squared_error:
Measures prediction error.

r2_score:
Measures model accuracy/performance.

6. df = pd.read_csv("BostonHousing.csv")
Reads Boston Housing dataset and stores it into DataFrame df.

Boston Housing Dataset:
Contains house-related features and house prices.

7. print(df.head())
Displays first 5 rows of dataset.

Purpose:
Quickly inspect dataset structure.

8. df = df.fillna(df.mean())
Handles missing values.

fillna():
Replaces missing values.

df.mean():
Uses column mean to fill missing numerical values.

Purpose:
Prevent errors during model training.

9. X = df.drop("MEDV", axis=1)
Creates independent variables/features.

drop("MEDV"):
Removes target column.

axis=1:
Indicates column removal.

X contains:
All input features except MEDV.

10. y = df["MEDV"]
Creates dependent variable/target variable.

MEDV:
Median house value (house price).

Purpose:
This is the value the model predicts.

11. train_test_split(...)
Splits dataset into training and testing data.

Parameters:
X:
Input features

y:
Target values

test_size=0.2:
20% data used for testing.

80% data used for training.

random_state=0:
Ensures same random split every run.

12. model = LinearRegression()
Creates Linear Regression model object.

Linear Regression:
Predicts values using linear relationship between variables.

Equation of Linear Regression:


::contentReference[oaicite:0]{index=0}


Where:
y = predicted value
m = slope
x = input feature
c = intercept

13. model.fit(X_train, y_train)
Trains the model.

fit():
Model learns relationship between:
- input features
- target values

14. y_pred = model.predict(X_test)
Predicts house prices for test data.

predict():
Uses learned model to generate predictions.

15. mean_squared_error(y_test, y_pred)
Calculates Mean Squared Error (MSE).

Purpose:
Measures prediction error.

Formula:

:contentReference[oaicite:1]{index=1}

Lower MSE:
Better model performance.

16. r2_score(y_test, y_pred)
Calculates R² Score.

Purpose:
Measures how well model explains data.

Range:
0 to 1

Closer to 1:
Better model.

17. import matplotlib.pyplot as plt
Imports Matplotlib for visualization.

18. plt.scatter(y_test, y_pred)
Creates scatter plot.

x-axis:
Actual prices

y-axis:
Predicted prices

Purpose:
Compare actual vs predicted values.

19. plt.xlabel("Actual Price")
Adds label to x-axis.

20. plt.ylabel("Predicted Price")
Adds label to y-axis.

21. plt.title("Actual vs Predicted Prices")
Adds graph title.

22. plt.plot(y_test, y_test)
Draws ideal prediction line.

Meaning:
If predictions are perfect,
all points lie on this line.

23. plt.show()
Displays graph.



================ IMPORTANT MACHINE LEARNING TERMS ================

Independent Variables:
Input features used for prediction.

Examples:
- crime rate
- rooms
- tax
- age

Dependent Variable:
Output/target variable.

Here:
MEDV (house price)

Training Data:
Used to teach model.

Testing Data:
Used to evaluate model performance.

Prediction:
Estimated output generated by model.



================ ABOUT BOSTON HOUSING DATASET ================

Boston Housing dataset contains information about houses in Boston.

Common columns:
CRIM   -> Crime rate
RM     -> Average number of rooms
AGE    -> Age of houses
TAX    -> Property tax
LSTAT  -> Lower status population
MEDV   -> Median house price

Goal:
Predict house prices using house features.



================ FINAL CONCLUSION ================

This program builds a Linear Regression model using the Boston Housing dataset.

Operations performed:
- dataset loading
- missing value handling
- train-test splitting
- model training
- prediction
- error calculation
- performance evaluation
- visualization of actual vs predicted prices
